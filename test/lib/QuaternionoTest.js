import test from 'tape';
import Quaterniono from '../../src/lib/Quaterniono.js';

test.test('Quaterniono.set test', function (t) {
	t.plan( 1 );
	t.equals( 1, 1 );
});

test.test('Quaterniono.rotate test', function (t) {
	t.plan( 1 );
	t.equals( 1, 1 );
});

test.test('Quaterniono.rotatePoint test', function (t) {
	t.plan( 1 );
	t.equals( 1, 1 );
});

test.test('Quaterniono.conjugate test', function (t) {
	t.plan( 1 );
	t.equals( 1, 1 );
});

test.test('Quaterniono.multiply test', function (t) {
	t.plan( 1 );
	t.equals( 1, 1 );
});

test.test('Quaterniono.toMatrix test', function (t) {
	t.plan( 1 );
	t.equals( 1, 1 );
});

test.test('Quaterniono.normalize test', function (t) {
	t.plan( 1 );
	t.equals( 1, 1 );
});

test.test('Quaterniono.length test', function (t) {
	t.plan( 1 );
	t.equals( 1, 1 );
});

test.test('Quaterniono.scale test', function (t) {
	t.plan( 1 );
	t.equals( 1, 1 );
});

test.test('Quaterniono.dot test', function (t) {
	t.plan( 1 );
	t.equals( 1, 1 );
});

test.test('Quaterniono.add test', function (t) {
	t.plan( 1 );
	t.equals( 1, 1 );
});

test.test('Quaterniono.subtract test', function (t) {
	t.plan( 1 );
	t.equals( 1, 1 );
});

test.test('Quaterniono.interpolateLinearly test', function (t) {
	t.plan( 1 );
	t.equals( 1, 1 );
});

test.test('Quaterniono.slerp test', function (t) {
	t.plan( 1 );
	t.equals( 1, 1 );
});
