import test from 'tape';
import Vectoro from '../../src/lib/Vectoro.js';

test.test('Vectoro.copy test', function (t) {
	t.plan( 1 );
	t.equals( 1, 1 );
});

test.test('Vectoro.scale test', function (t) {
	t.plan( 1 );
	t.equals( 1, 1 );
});

test.test('Vectoro.normalize test', function (t) {
	t.plan( 1 );
	t.equals( 1, 1 );
});

test.test('Vectoro.length test', function (t) {
	t.plan( 1 );
	t.equals( 1, 1 );
});

test.test('Vectoro.length2 test', function (t) {
	t.plan( 1 );
	t.equals( 1, 1 );
});

test.test('Vectoro.distance test', function (t) {
	t.plan( 1 );
	t.equals( 1, 1 );
});

test.test('Vectoro.distance2 test', function (t) {
	t.plan( 1 );
	t.equals( 1, 1 );
});

test.test('Vectoro.dot test', function (t) {
	t.plan( 1 );
	t.equals( 1, 1 );
});

test.test('Vectoro.cross test', function (t) {
	t.plan( 1 );
	t.equals( 1, 1 );
});

test.test('Vectoro.subtract test', function (t) {
	t.plan( 1 );
	t.equals( 1, 1 );
});

test.test('Vectoro.normalVector test', function (t) {
	t.plan( 1 );
	t.equals( 1, 1 );
});

test.test('Vectoro.add test', function (t) {
	t.plan( 1 );
	t.equals( 1, 1 );
});

test.test('Vectoro.toString test', function (t) {
	t.plan( 1 );
	t.equals( 1, 1 );
});
