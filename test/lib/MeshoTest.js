import test from 'tape';
import Mesho from '../../src/lib/Mesho.js';

test.test('Mesho.triangulate test', function (t) {
	t.plan( 1 );
	t.equals( 1, 1 );
});

test.test('Mesho.normals test', function (t) {
	t.plan( 1 );
	t.equals( 1, 1 );
});

test.test('Mesho.uniqVertices test', function (t) {
	t.plan( 1 );
	t.equals( 1, 1 );
});

test.test('Mesho.facesToEdges test', function (t) {
	t.plan( 1 );
	t.equals( 1, 1 );
});

test.test('Mesho.normalize test', function (t) {
	t.plan( 1 );
	t.equals( 1, 1 );
});

test.test('Mesho.axis test', function (t) {
	t.plan( 1 );
	t.equals( 1, 1 );
});
